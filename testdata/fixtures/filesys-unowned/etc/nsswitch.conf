# The local files are the whole account database on this host. That is what
# lets FILESYS-0010 conclude that uid 4242 belongs to nobody rather than
# suspecting it might be served from a directory this offline scan cannot ask.
passwd:     files
group:      files
shadow:     files

hosts:      files dns
