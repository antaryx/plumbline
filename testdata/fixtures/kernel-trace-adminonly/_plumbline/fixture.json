{ "description": "ptrace_scope at 2, which allows tracing only to a process holding CAP_SYS_PTRACE. Anything above 0 passes: the right level depends on what the host runs, and a system that chose 2 deliberately is not the problem the check exists to find. The verdict has to say what 2 restricts rather than repeating the number, which is what separates a useful pass from a tick." }
