{ "description": "Every parameter written down and written down wrong — a tuning drop-in that disabled SYN cookies and reverse-path filtering and enabled source routing and ICMP redirects. Distinguishes a decision somebody made from a default nobody set, which the findings word differently." }
