{
  "description": "Two drop-ins with the same filename in two search directories. systemd takes the one from the higher-precedence directory and ignores the other entirely, so the tcp:// binding in /lib is never applied and this host is not exposed. A collector that applied every .conf it found would fail CONTAINERS-0006 here, which would be a critical false positive on a host somebody had deliberately fixed."
}
