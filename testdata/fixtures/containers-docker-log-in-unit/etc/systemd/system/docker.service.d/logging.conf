# Written by configuration management: the log settings live with the unit
# because this fleet's daemon.json is owned by a different role.
[Service]
ExecStart=
ExecStart=/usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.sock --log-driver=json-file --log-opt max-size=50m --log-opt max-file=5
