{ "description": "Both information-leak parameters persisted at their strongest values, in the one file the remediation recommends: kptr_restrict at 2 hides pointers from privileged readers too, and dmesg_restrict at 1 needs CAP_SYSLOG for the ring buffer. The running kernel agrees with the file, so KERNEL-0007 is quiet and the two persistence checks are the only ones with anything to say." }
