{ "description": "The other half of the runtime cross-reference, and the case the downgrade must never reach: nothing is written down and the running kernel is exposed on all three. Identical in its configuration to kernel-fs-unset — the same drop-in, mentioning none of them — and differing only in /proc. That is what makes the pair prove the tiering reads the runtime rather than the file: same file, opposite severity." }
