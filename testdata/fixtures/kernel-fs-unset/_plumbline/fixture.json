{ "description": "The common real-world shape and the one these checks exist for: the running kernel is at its own safe defaults for all three, and no file on the host records that anyone chose them. Every bundle in the corpus except Alpine and Ubuntu looks like this. A default is not a decision." }
