#!/usr/sbin/nft -f
flush ruleset

table inet filter {
	chain input {
		type filter hook input priority 0; policy accept;

		# Block the ports we know about.
		tcp dport 23 drop
		tcp dport 512-514 drop
	}
}
