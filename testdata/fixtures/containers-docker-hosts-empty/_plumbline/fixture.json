{
  "description": "hosts written down and left empty. It binds nothing, so it passes, and it is not the same position as a host that never wrote the key: this operator considered the sockets and asked for none, that one left them to the systemd unit. The fact carries hosts as a []string, where nil and [] are the same value, so the distinction survives only because Keys records which top-level keys the document set - this fixture is what proves the check reads it."
}
