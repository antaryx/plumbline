{
  "description": "Everything this module models is hardened except icc, which is explicitly true. It is the isolating fixture for CONTAINERS-0003: 0001 and 0002 pass over it and only 0003 fails, which is what proves the three checks read the option each owns rather than a shared notion of whether the daemon looks configured."
}
