kernel.yama.ptrace_scope = 1
# Sync and remount read-only only: what the operations team uses to recover a
# wedged host from the console. No dumps, no signalling, no log-level control.
kernel.sysrq = 48
