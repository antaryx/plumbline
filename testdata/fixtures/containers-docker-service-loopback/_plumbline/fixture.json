{
  "description": "The API bound to loopback and nothing else. Not reachable from the network and reachable by every local user and every --network=host container, which is why CONTAINERS-0006 rates it below a routable binding and still fails it. The pair with containers-docker-service-tcp is what proves the check reads the address rather than the scheme."
}
