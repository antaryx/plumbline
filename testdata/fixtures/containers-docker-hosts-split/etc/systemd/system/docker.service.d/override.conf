# Without this the daemon refuses to start: hosts is set in daemon.json and the
# vendor unit passes -H, and dockerd will not take one option from two places.
[Service]
ExecStart=
ExecStart=/usr/bin/dockerd --containerd=/run/containerd/containerd.sock
