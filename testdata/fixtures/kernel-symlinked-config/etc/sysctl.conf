# The traditional file. On Debian and Ubuntu it is reached twice: once by name,
# and once through the 99-sysctl.conf link that makes it sort last.
kernel.unprivileged_bpf_disabled = 1
net.core.bpf_jit_harden = 2
kernel.kptr_restrict = 1
