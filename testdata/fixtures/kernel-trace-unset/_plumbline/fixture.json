{ "description": "Neither parameter written to any file, with a running kernel that has ptrace_scope at 1 — the trap these persistence checks exist for. KERNEL-0003 passes on the running value, KERNEL-0007 sees nothing to compare, and the host reverts at the next reboot. The running sysrq is 176 so the failure can quote what is currently enabled while still failing on the absence." }
