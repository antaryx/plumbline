# Ship everything to the central collector. The queue is what lets this host
# survive a collector outage without discarding messages.
action(type="omfwd"
       target="logs.example.net"
       port="514"
       protocol="tcp"
       queue.type="linkedlist"
       queue.filename="fwd"
       action.resumeRetryCount="-1")
