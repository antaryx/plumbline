# The distribution's narrower mask. Same directory, later filename: both procps
# and systemd-sysctl apply this after 50-default.conf, so 176 is the answer and
# there is nothing to be uncertain about.
kernel.sysrq = 176
