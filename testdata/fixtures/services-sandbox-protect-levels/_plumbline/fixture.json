{
  "description": "The three enum levels in one host: journald at strict with an explicit ReadWritePaths, dbus at full, cron at yes. All three pass, which is the point — the check's bar is yes and upward, not strict, because the right level depends on what the daemon legitimately writes. cron sets it despite being exempt, so this also proves an exemption is a floor and not a ceiling."
}
