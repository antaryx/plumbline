{ "description": "A mask that looks like the narrow one and is not: 184 adds bit 8, the debugging dumps, to the recovery functions. Those print registers, memory and every task's stack to the console, defeating address-space randomisation in one keystroke — so this stays at MEDIUM where 48 drops to LOW. It is the fixture that proves the tier turns on which bits are set rather than on how many." }
