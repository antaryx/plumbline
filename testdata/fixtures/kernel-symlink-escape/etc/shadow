# Not a shadow file: a stand-in for one, holding no crypt-shaped string, so
# that a secret scanner reading this repository has nothing to report. What the
# test asserts is which seam method read these bytes, not what they say.
root:LOCKED-PLACEHOLDER-NOT-A-HASH:19000:0:99999:7:::
