{
  "description": "/usr/bin/sudo is ET_EXEC with an executable stack and no PT_GNU_RELRO segment: it loads at a fixed address, so ASLR cannot randomise its text and the addresses an attacker needs are the same on every host running this build. The other three are fully hardened, which is what makes this the fixture that proves the checks name the offender rather than reporting the host as broken."
}
