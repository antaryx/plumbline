#!/usr/sbin/nft -f
# This file is installed by the nftables package. It does not contain a
# ruleset; write one here and enable nftables.service to load it at boot.
#
#   flush ruleset
#   table inet filter {
#     chain input { type filter hook input priority 0; policy drop; }
#   }
