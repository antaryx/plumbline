# Name Service Switch configuration. The local files are the whole account
# database on this host, which is what lets FILESYS-0010 return a verdict
# rather than UNKNOWN.
passwd:     files
group:      files
shadow:     files

hosts:      files dns
